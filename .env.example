# CF Training Companion API — Environment Variables
# Copy this file to .env and fill in your values

# ─── Database ────────────────────────────────────────────────────────────────
# Leave CLOUD_SQL_CONNECTION_NAME blank to use SQLite locally
CLOUD_SQL_CONNECTION_NAME=
DB_USER=postgres
DB_PASSWORD=
DB_NAME=cf_log

# Path to SQLite DB file (used only when CLOUD_SQL_CONNECTION_NAME is not set)
# Defaults to ./cf_log.db if not set
CFLOG_DB=

# ─── CORS ────────────────────────────────────────────────────────────────────
# Comma-separated list of allowed origins for the Next.js dashboard
# Local development default is http://localhost:3000
# Production example: https://cf-dashboard.vercel.app,https://myapp.com
ALLOWED_ORIGINS=http://localhost:3000

# ─── Rate Limiting ───────────────────────────────────────────────────────────
RATE_LIMIT_REQUESTS=300
RATE_LIMIT_WINDOW=60
